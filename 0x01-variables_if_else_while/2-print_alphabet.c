#include<stdio.h>

/**
* main - Entry point.
* Return: 0 always if successful.
**/

int main(void)
{
int letter;
for (letter = 97; letter < 123; letter++)
putchar(letter);
putchar('\n');
 return (0);
}
