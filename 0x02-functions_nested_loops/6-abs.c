/**
* _abs - Returns the absolute value of v.
* @v: The integer whose absolute value is to be found.
* Return: Returns a positive number.
**/

int _abs(int v)
{
if (v < 0)
return (v / -1);
else
return (v);
}
